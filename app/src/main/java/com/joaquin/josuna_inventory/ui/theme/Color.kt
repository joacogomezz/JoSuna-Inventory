package com.joaquin.josuna_inventory.ui.theme

import androidx.compose.ui.graphics.Color

// Dark base
val Background = Color(0xFF0D0D0F)
val Surface = Color(0xFF1A1A1E)
val SurfaceElevated = Color(0xFF242428)
val SurfaceBorder = Color(0xFF2E2E34)

// Light base
val BackgroundLight = Color(0xFFF5F5F5)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceElevatedLight = Color(0xFFEEEEEE)
val SurfaceBorderLight = Color(0xFFDDDDDD)

// Accent — naranja industrial
val Primary = Color(0xFFFF6B2B)
val PrimaryDim = Color(0xFF3D2010)
val PrimaryContainer = Color(0xFF2A1608)

// Accent claro
val PrimaryDimLight = Color(0xFFFFE0D0)
val PrimaryContainerLight = Color(0xFFFFD0B0)

// Texto dark
val OnBackground = Color(0xFFF0F0F0)
val OnSurface = Color(0xFFE0E0E0)
val OnSurfaceVariant = Color(0xFF8A8A96)
val OnSurfaceDim = Color(0xFF55555F)

// Texto light
val OnBackgroundLight = Color(0xFF1A1A1A)
val OnSurfaceLight = Color(0xFF2A2A2A)
val OnSurfaceVariantLight = Color(0xFF666666)
val OnSurfaceDimLight = Color(0xFF999999)

// Semánticos
val Success = Color(0xFF2ECC71)
val Error = Color(0xFFE74C3C)
val Warning = Color(0xFFFFB300)
